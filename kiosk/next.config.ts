import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // The kiosk runs as a single-purpose fullscreen app; no special config needed.
  reactStrictMode: true,
};

export default nextConfig;
